{
  "name": "chore-quest",
  "version": "1.0.0",
  "description": "Gamified household chore tracker via SMS + Google Sheets + Cloudflare",
  "private": true,
  "scripts": {
    "dev": "cd worker && wrangler dev",
    "deploy:worker": "cd worker && wrangler deploy",
    "deploy:dashboard": "wrangler pages deploy dashboard --project-name chore-quest",
    "deploy": "npm run deploy:worker && npm run deploy:dashboard",
    "setup:sheets": "cd sheets-setup && node setup-sheet.js"
  },
  "workspaces": [
    "worker"
  ]
}
